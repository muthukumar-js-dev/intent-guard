# Layer Violation Example

This project contains intentional architectural violations.
- **Domain** layer imports **Infrastructure** (Strictly forbidden).

Run `intent-guard validate` to see the error report.

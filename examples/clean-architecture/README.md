# Clean Architecture Example

This project demonstrates a valid architecture with 3 layers:
1. **Domain**: The core, no dependencies.
2. **Application**: Depends on Domain.
3. **Infrastructure**: Depends on Application and Domain.

Run `intent-guard validate` here to see a successful validation.
